<%# Search frame - Work in progress %>
<%= turbo_frame_tag "search", class: "shadow overflow-hidden rounded border-b border-gray-200" do %>
  <div class="flex justify-end mb-1">
    <%= form_with url: list_normanpds_path, method: :get do |form| %>
      <%= form.text_field :business, placeholder: "Search by name", value: params[:business], class: "border border-blue-500 rounded p-2" %>
      <%= form.button "Search", class: "bg-blue-500 text-white p-2 rounded-sm" %>
    <% end %>
  </div>
  <!-- Snip the table -->
<% end %>